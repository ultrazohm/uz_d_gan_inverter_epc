\documentclass{article}
\usepackage[fulldiodes]{circuitikz}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{circuitikz}
\usepackage{graphicx}
\usetikzlibrary{shapes.geometric}

\begin{document}
	
	\centering
	
	
	\begin{circuitikz}
		
		\centering
		
		\ctikzset{bipoles/resistor/height=0.15}
		\ctikzset{bipoles/resistor/width=0.4}
		\ctikzset{resistor = european}
		
		
		\draw (0,0) to ++(1.5, 0) 
		to[/tikz/circuitikz/bipoles/length=25pt, R = $R_{DC}$]  ++(1.5, 0 );
		\draw (6,0) to[short,i=$i$] ++(-3, 0 ) ;
		\path (0,0) -- node[auto=false]{\ldots} ++(-1,0);
		\path (6,0) -- node[auto=false]{\ldots} ++(1,0);
		
		
		
		\draw (4.4,2) node[op amp, ] (opamp) {};
		\draw (opamp.+) to[short] ++(0,-1.5) to[short, -*]  ++(0,0) node[below]{Sense+};
		\draw (opamp.-) to[short] ++(-1.8,0) coordinate (left) to[short, -*] ++(0,-2.5) node[below]{Sense-};; 
		
		
		\draw [->] (opamp.down) node[]{} to ++(2,0) node[right] {$$};
		\draw [->] (opamp.up) to ++(0.7,0) node[above] {$OCP$}
		to ++(1.2,0) coordinate(sq);
		\node[rectangle,draw] (r) at (7,2.5) {FPGA};
		\node[rectangle,draw] (r) at (7,1.5) {ADC};
		
		
		
		
		
		
	\end{circuitikz}
	
	
	
	
\end{document}